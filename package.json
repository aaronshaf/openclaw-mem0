{
  "name": "openclaw-mem0",
  "private": true,
  "workspaces": ["plugin", "server"],
  "scripts": {
    "test": "cd server && bun test",
    "test:all": "cd server && bun test && cd ../plugin && bun test"
  }
}
