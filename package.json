{
  "name": "openclaw-mem0-rest",
  "version": "0.1.0",
  "description": "openclaw memory plugin — calls mem0 REST API. Simple, no native deps.",
  "main": "index.ts",
  "scripts": {},
  "dependencies": {
    "effect": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
